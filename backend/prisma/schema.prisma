generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Phase 1: Foundation only.
// We'll add models (User, Article, Feedback, etc.) in Phase 2.

