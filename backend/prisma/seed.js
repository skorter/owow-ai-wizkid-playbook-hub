// Phase 2 placeholder seed file.
// We'll add real seeding once database models exist.

async function main() {
  console.log("Prisma seed placeholder: no models to seed yet.");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});

