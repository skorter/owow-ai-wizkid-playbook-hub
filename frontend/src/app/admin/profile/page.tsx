import AdminPageContainer from "@/components/admin/AdminPageContainer/AdminPageContainer";

export default function ProfilePage() {
  return (
    <AdminPageContainer title="Profile" subtitle="Your admin account settings.">
      <p>Profile settings will be available in a later phase.</p>
    </AdminPageContainer>
  );
}
